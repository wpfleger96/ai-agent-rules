name: personal
description: Personal configuration with additional tools and integrations
extends: default
settings_overrides:
  claude:
    model: opusplan
    env:
      DISABLE_COMPACT: "1"
  statusline:
    terminal_title:
      enabled: true
exclude_symlinks: []
mcp_overrides: {}
